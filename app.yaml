runtime: python
instance_class: F4
env: flex
entrypoint: gunicorn -b :$PORT app:app

liveness_check:
  initial_delay_sec: 420

readiness_check:
  app_start_timeout_sec: 600

handlers:
- url: /public
  static_dir: public

runtime_config:
  python_version: 3


